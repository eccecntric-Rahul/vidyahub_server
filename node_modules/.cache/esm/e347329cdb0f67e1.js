let express,mongoose,fs,cors,formidable;_db0‍.w("express",[["default",["express"],function(v){express=v}]]);_db0‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_db0‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_db0‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_db0‍.w("express-formidable",[["default",["formidable"],function(v){formidable=v}]]);
const morgan = require("morgan");




require("dotenv").config();
const AutoIncrementFactory = require('mongoose-sequence');

const app = express();
const port=process.env.PORT||8000;

// to communicate between cross origins 
app.use(cors());

// http request logger
app.use(morgan("dev"));

// for json object Data
app.use(express.json());



app.use(formidable());

// db connection 
const connection =mongoose.connect(process.env.URI,{useNewUrlParser:true,useFindAndModify:false,useUnifiedTopology:true,useCreateIndex:true,});
// .then(()=>console.log("db connected"))
// .catch((err)=>console.log("DB connection error"));

const AutoIncrement = AutoIncrementFactory(connection);

fs.readdirSync("./routes").map((r)=>app.use("/api",require(`./routes/${r}`)));

app.listen(port,()=>console.log(`Server is running on Port ${port}`) );